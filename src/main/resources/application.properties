server.port=8100
# FIXME: Follow instructions to setup MySQL and add the following properties
spring.datasource.url=jdbc:mysql://${LOCAL_SERVER:localhost}:3306/${MYSQL_DATABSASE:reviews}
spring.datasource.username=${MYSQL_USER:user}
spring.datasource.password=${MYSQL_PASSWORD:password}

spring.jpa.show-sql=true

# DO NOT MODIFY
# Don't let JPA/Hibernate create the schema
spring.jpa.generate-ddl=false
spring.jpa.hibernate.ddl-auto=none